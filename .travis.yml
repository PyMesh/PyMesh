language: cpp
sudo: false
python:
    - "2.7
matrix:
  include:
    - os: osx
      compiler: clang
      install:
        - brew update
        - brew upgrade cmake
        - brew upgrade cgal
        - git submodule update --init --recursive
        - mkdir third_party/build
        - cd third_party/build
        - cmake ..
        - make
        - make install
      script:
        - echo $pwd
        - mkdir build
        - cd build
        - cmake ..
        - make
        - make tools

