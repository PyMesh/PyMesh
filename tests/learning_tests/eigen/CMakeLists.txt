# Include headers
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# Enumerate source files
FILE(GLOB TEST_SRC_FILES *.cpp)
FILE(GLOB TEST_INC_FILES *.h)

ADD_EXECUTABLE(eigen_tests ${TEST_SRC_FILES} ${TEST_INC_FILES})
TARGET_LINK_LIBRARIES(eigen_tests ${GTEST_BOTH_LIBRARIES} Mesh)
ADD_CUSTOM_COMMAND(TARGET eigen_tests POST_BUILD COMMAND eigen_tests)
