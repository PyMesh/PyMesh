#pragma once
#include <string>
#include <vector>

namespace Functions {
    std::string output(const std::string& str);
    std::vector<double> x2(const std::vector<double>& arr);
    std::vector<double> x3(const std::vector<double>& arr);
}
