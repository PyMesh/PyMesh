# Include headers
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/tools)

# CGAL
IF (CGAL_FOUND)
    ADD_DEFINITIONS(-DWITH_CGAL)
ENDIF (CGAL_FOUND)

# Geogram
IF (GEOGRAM_FOUND)
    ADD_DEFINITIONS(-DWITH_GEOGRAM)
ENDIF (GEOGRAM_FOUND)

# IGL
IF (LIBIGL_FOUND)
    ADD_DEFINITIONS(-DWITH_IGL)
ENDIF (LIBIGL_FOUND)

# Enumerate source files
FILE(GLOB_RECURSE TEST_SRC_FILES *.cpp)
FILE(GLOB_RECURSE TEST_INC_FILES *.h *.inl)

ADD_EXECUTABLE(bvh_tests ${TEST_SRC_FILES} ${TEST_INC_FILES})
TARGET_LINK_LIBRARIES(bvh_tests ${GTEST_BOTH_LIBRARIES} Mesh lib_bvh)
ADD_CUSTOM_COMMAND(TARGET bvh_tests POST_BUILD COMMAND bvh_tests)
