# Include headers
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/tools)

# Enumerate source files
FILE(GLOB_RECURSE TEST_SRC_FILES *.cpp)
FILE(GLOB_RECURSE TEST_INC_FILES *.h *.inl)

IF(draco_FOUND)
    ADD_DEFINITIONS(-DWITH_DRACO)
ENDIF(draco_FOUND)

ADD_EXECUTABLE(compression_tests ${TEST_SRC_FILES} ${TEST_INC_FILES})
TARGET_LINK_LIBRARIES(compression_tests ${GTEST_BOTH_LIBRARIES} Mesh
    lib_compression)
ADD_CUSTOM_COMMAND(TARGET compression_tests POST_BUILD COMMAND compression_tests)
