# Include headers
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/tools)

# Cork
IF (CORK_FOUND)
    INCLUDE_DIRECTORIES(${CORK_INCLUDE_DIRS})
    ADD_DEFINITIONS(-DWITH_CORK)
ENDIF (CORK_FOUND)

# Clipper
IF(CLIPPER_FOUND)
    INCLUDE_DIRECTORIES(${CLIPPER_INCLUDE_DIRS})
    ADD_DEFINITIONS(-DWITH_CLIPPER)
ENDIF(CLIPPER_FOUND)

# IGL
IF(LIBIGL_FOUND AND CGAL_FOUND)
    INCLUDE_DIRECTORIES(${IGL_INCLUDE_DIRS})
    ADD_DEFINITIONS(-DWITH_IGL)
ENDIF(LIBIGL_FOUND AND CGAL_FOUND)

# Enumerate source files
FILE(GLOB_RECURSE TEST_SRC_FILES *.cpp)
FILE(GLOB_RECURSE TEST_INC_FILES *.h *.inl)

ADD_EXECUTABLE(csg_tests ${TEST_SRC_FILES} ${TEST_INC_FILES})
TARGET_LINK_LIBRARIES(csg_tests ${GTEST_BOTH_LIBRARIES} Mesh lib_csg
    lib_MeshUtils)
ADD_CUSTOM_COMMAND(TARGET csg_tests POST_BUILD COMMAND csg_tests)
