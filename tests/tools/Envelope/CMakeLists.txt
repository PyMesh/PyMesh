# Include headers
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/tools)

# Enumerate source files
FILE(GLOB_RECURSE TEST_SRC_FILES *.cpp)
FILE(GLOB_RECURSE TEST_INC_FILES *.h *.inl)

ADD_EXECUTABLE(envelope_tests ${TEST_SRC_FILES} ${TEST_INC_FILES})
TARGET_LINK_LIBRARIES(envelope_tests ${GTEST_BOTH_LIBRARIES} Mesh lib_MeshUtils)
ADD_CUSTOM_COMMAND(TARGET envelope_tests POST_BUILD COMMAND envelope_tests)
IF(NOT WITHOUT_SWIG)
#ADD_CUSTOM_COMMAND(TARGET envelope_tests POST_BUILD COMMAND
#    "${CMAKE_CURRENT_SOURCE_DIR}/unit_test_driver.py")
ENDIF(NOT WITHOUT_SWIG)
