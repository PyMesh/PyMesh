# Add python binding.
INCLUDE_DIRECTORIES(${EIGEN_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${EIGEN_INCLUDE_DIRS}/unsupported)
PYBIND11_ADD_MODULE(PyMesh PyMesh.cpp)
TARGET_LINK_LIBRARIES(PyMesh PRIVATE Mesh)
